"""Preset-specific test suite. Each file validates one shipped preset in
``portfolios/`` against the engine's invariants and (for presets that mirror
legacy module-level configuration) against those globals."""
